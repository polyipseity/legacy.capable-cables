//////////////////////////////////////////////////
// TITLE Miscellaneous
//////////////////////////////////////////////////


// COMMENT arguments

project.ext.parseArgument = { CharSequence k, String envK, Object defaultV = null ->
	String k2 = k.toString()
	project.hasProperty(k2) ? property(k2) : System.getenv(envK) != null ? System.getenv(envK) : defaultV
}
