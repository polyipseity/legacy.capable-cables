//////////////////////////////////////////////////
// TITLE Repositories
//////////////////////////////////////////////////

import static java.io.File.separator


/* SECTION check */

apply from: "$utilitiesDir${separator}meta${separator}application.gradle"
checkApply.call(['stringToStringMap', 'evaluateGStringString'])


/* SECTION configuration */

repositories {
    gradlePluginPortal()
    jcenter()
    mavenCentral()
    mavenLocal()
    google()
}

stringToStringMap.call(customRepositories).each { String k, List<String> v ->
    v.each {
        String[] e = it.split('(?<!\\\\):', 2).collect { evaluateGStringString.call it.trim().replace('\\:', ':') }
        repositories."$k" {
            name = e.first()
            url = e.last()
        }
    }
}
